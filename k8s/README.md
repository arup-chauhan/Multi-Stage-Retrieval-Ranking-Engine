# Kubernetes Skeleton

This directory contains initial manifests for eventual EKS-ready deployment.

Current scope:
- service deployments + cluster services for query/vector/ranking
- infra deployments for redis and solr
- HPA placeholders for service autoscaling

Use this as a starting point and tune images/resources per environment.
